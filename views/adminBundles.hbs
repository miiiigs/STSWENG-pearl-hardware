<!-- Button to Create New Bundle -->
        <button id="createBundleBtn" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded mb-4">
            Create New Bundle
        </button>

        <!-- List of Existing Bundles -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
            <!-- Loop through your bundles and display each one -->
            {{#each bundles}}
            <div class="bg-white shadow-md rounded-lg p-4">
                <h2 class="text-lg font-semibold">{{this.name}}</h2>
                <p class="text-gray-600">{{this.description}}</p>
                <div class="mt-4">
                    <p class="text-gray-800 font-bold">Price: ${{this.price}}</p>
                    <button class="bg-green-500 hover:bg-green-600 text-white font-bold py-1 px-2 rounded mt-2">Edit Price</button>
                </div>
            </div>
            {{/each}}
        </div>

        <!-- Modal for Creating/Editing Bundle -->
<div id="bundleModal" class="fixed inset-0 bg-black bg-opacity-50 flex justify-center items-center hidden">
    <div class="bg-white p-8 rounded shadow-md w-96">
        <h1 class="text-2xl font-semibold mb-4" id="bundleModalTitle">Create New Bundle</h1>

        <!-- Form for Creating/Editing Bundle -->
        <form id="bundlesForm">
            <div class="mb-4">
                <label for="bundleName" class="block text-sm font-medium text-gray-600">Bundle Name</label>
                <input type="text" id="bundleName" name= "name" class="mt-1 p-2 w-full border rounded">
            </div>

            <div class="mb-6">
                <label for="bundleDescription" class="block text-sm font-medium text-gray-600">Description</label>
                <textarea id="bundleDescription" name= "description" class="mt-1 p-2 w-full border rounded"></textarea>
            </div>

            <div class="mb-6">
                <label for="bundlePrice" class="block text-sm font-medium text-gray-600">Price</label>
                <input type="number" id="bundlePrice" name= "price" class="mt-1 p-2 w-full border rounded">
            </div>

            <div class="mb-6">
                <label class="block text-sm font-medium text-gray-600">Select Products</label>
                <div class="grid grid-cols-1 gap-2 mt-2 max-h-60 overflow-y-auto" id="productList">
                    <!-- Products will be dynamically added here -->
                </div>
            </div>

            <div class="mt-4">
                <button type="submit" id="saveBundleBtn" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded">
                    Save Bundle
                </button>
                <button type="button" id="closeModalBtn" class="bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 rounded ml-2">
                    Cancel
                </button>
            </div>
        </form>
    </div>